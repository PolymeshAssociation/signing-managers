{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "signing-manager",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Package name (without the org prefix)",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
