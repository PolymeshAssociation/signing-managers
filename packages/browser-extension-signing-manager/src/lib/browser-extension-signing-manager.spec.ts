import { browserExtensionSigningManager } from './browser-extension-signing-manager';

describe('browserExtensionSigningManager', () => {
  it('should work', () => {
    expect(browserExtensionSigningManager()).toEqual('browser-extension-signing-manager');
  });
});
